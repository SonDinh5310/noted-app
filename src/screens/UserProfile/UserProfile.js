import React from 'react';
import { Text, View } from 'react-native';

const UserProfile = () => {
    return (
        <View>
            <Text>This is user's profile</Text>
        </View>
    );
};

export default UserProfile;
